import {groupVotes,type Vote} from '../lib/votes';
type Props={options:string[];votes:Vote[];correctIndex:number;currentParticipantId?:string};
export function VoteGroups({options,votes,correctIndex,currentParticipantId}:Props){const groups=groupVotes(options,votes,correctIndex);return <div className="vote-groups">{groups.map(group=><section key={group.optionIndex} className={`vote-group ${group.isCorrect?'correct':''}`}><header><span>0{group.optionIndex+1}</span><div><h3>{group.option}</h3>{group.isCorrect&&<small>ПРАВИЛЬНЫЙ ОТВЕТ</small>}</div><b>{group.voters.length}</b></header><div className="voter-list">{group.voters.length===0?<span className="empty-voters">Никто не выбрал</span>:group.voters.map((vote,index)=><span key={vote.participantId??`${vote.name}-${index}`} className={vote.participantId===currentParticipantId?'current-user':''}>{vote.name}{vote.participantId===currentParticipantId&&<small>ВЫ</small>}</span>)}</div></section>)}</div>}
