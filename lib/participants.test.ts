import {describe,expect,it} from 'vitest';import {participantLabels} from './participants';describe('participantLabels',()=>{it('keeps unique names',()=>expect(participantLabels([{id:'1',name:'Аня'}]).get('1')).toBe('Аня'));it('disambiguates duplicate names case-insensitively and by whitespace',()=>{const labels=participantLabels([{id:'1',name:' Аня '},{id:'2',name:'аня'},{id:'3',name:'Аня   Иванова'}]);expect(labels.get('1')).toBe('Аня · 1');expect(labels.get('2')).toBe('аня · 2');expect(labels.get('3')).toBe('Аня Иванова')})});
