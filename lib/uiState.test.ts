import {describe,expect,it,vi} from 'vitest';import {mapQuizError} from './uiState';describe('mapQuizError',()=>{it('maps RPC codes',()=>{vi.spyOn(console,'error').mockImplementation(()=>{});expect(mapQuizError(new Error('answer_already_submitted'))).toBe('Ваш ответ уже принят');expect(mapQuizError(new Error('invalid_transition'))).toContain('недоступно')});it('maps network errors',()=>{vi.spyOn(console,'error').mockImplementation(()=>{});expect(mapQuizError(new Error('Failed to fetch'))).toContain('Нет соединения')})});
